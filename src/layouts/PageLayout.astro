---
import CourseLayout from './CourseLayout.astro';
const { frontmatter, ...direct } = Astro.props;
const page = frontmatter ?? direct;
---
<CourseLayout title={page.title} description={page.description}>
  <slot />
</CourseLayout>
